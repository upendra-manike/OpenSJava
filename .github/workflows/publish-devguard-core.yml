# Publishes io.github.upendra-manike:devguard-core to Maven Central (Sonatype Publisher API).
# Configure repository secrets before running:
#   MAVEN_USERNAME          — Central portal user token username
#   MAVEN_PASSWORD          — Central portal user token password
#   MAVEN_GPG_PRIVATE_KEY   — ASCII-armored private key
#   MAVEN_GPG_PASSPHRASE    — Passphrase for that key
name: Publish devguard-core
on:
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven
          server-id: central
          server-username: MAVEN_USERNAME
          server-password: MAVEN_PASSWORD
          gpg-private-key: ${{ secrets.MAVEN_GPG_PRIVATE_KEY }}
          gpg-passphrase: MAVEN_GPG_PASSPHRASE

      - name: Deploy to Central
        env:
          MAVEN_USERNAME: ${{ secrets.MAVEN_USERNAME }}
          MAVEN_PASSWORD: ${{ secrets.MAVEN_PASSWORD }}
          MAVEN_GPG_PASSPHRASE: ${{ secrets.MAVEN_GPG_PASSPHRASE }}
        run: mvn -B -pl devguard-core -Psign-loopback clean deploy
