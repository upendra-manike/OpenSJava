# Publishes io.github.upendra-manike:smart-cache to Maven Central (Sonatype Publisher API).
# Configure repository secrets before running:
#   MAVEN_USERNAME          — Central portal user token username
#   MAVEN_PASSWORD          — Central portal user token password
#   MAVEN_GPG_PRIVATE_KEY   — ASCII-armored private key (optional if you use a different signing flow)
#   MAVEN_GPG_PASSPHRASE    — Passphrase for that key
name: Publish smart-cache
on:
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven
          server-id: central
          server-username: MAVEN_USERNAME
          server-password: MAVEN_PASSWORD
          gpg-private-key: ${{ secrets.MAVEN_GPG_PRIVATE_KEY }}
          gpg-passphrase: ${{ secrets.MAVEN_GPG_PASSPHRASE }}

      - name: Deploy to Central
        env:
          MAVEN_USERNAME: ${{ secrets.MAVEN_USERNAME }}
          MAVEN_PASSWORD: ${{ secrets.MAVEN_PASSWORD }}
        run: mvn -B -pl smart-cache -Psign-loopback clean deploy
